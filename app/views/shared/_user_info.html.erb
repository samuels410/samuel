<div class="row">
<div class="span4">
	<a href="<%= user_path(current_user) %>">
		<%= gravatar_for current_user, size: 52 %>
	</a>
	<%= current_user.email %>
	<span>
		<%= link_to "view my profile", edit_user_registration_path %>
	</span>
	<span>
		<%= pluralize(current_user.posts.count, "Post") %>
	</span>
</div>
</div>